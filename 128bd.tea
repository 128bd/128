import tea.green.fmt
import tea.green.type

fn main() = int {
    var mut i: int16 = 0

    for _ {
        if i == 128 {
            break
        }

        fmt#println("128bd")

        i += 1
    }
}
